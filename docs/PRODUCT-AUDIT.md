# Velum product and engineering audit — September 12, 2026

Scope: live first-visit page, imported-invoice workflow, persistent storage, CRE
source boundary, Solidity treasury, Sepolia reconciliation, failure/retry behavior,
mobile layout, receipts, submission narrative and operational constraints. This is
an implementation review and targeted test exercise, not an independent security audit.

## Findings and changes

| Dimension | Evidence / weakness | Change or explicit limit |
|---|---|---|
| First impression | First-visit page measured 4,562 px high at 1440×1000, with eight h2 headings. Sepolia proof appeared at y=2609. | Move testnet proof near the opening, add a three-step route, collapse older local execution and single-invoice demos. |
| User purpose | Main CTA led to the preset comparison rather than the working accounting desk. | Point the CTA and first navigation item to the persistent payment desk. |
| Review safety | Imported invoices could only be evaluated by reserving budget. | Add read-only review with eligibility totals, held reasons and source revision. Editing invalidates review. |
| Multi-tab correctness | Existing storage serialization prevents budget overspend, but users could reserve a draft after the state they reviewed had changed. | Check expected ledger revision atomically when reserving; stale review must be refreshed. |
| Retry correctness | Retrying a POST created a new run with different IDs and decisions. | Client request key and canonical input hash return the original saved result on retry. A reused key with different rows is rejected. |
| Auditability | Only the latest decision text was easy to inspect; saved runs lacked downloadable artifacts. | Persist exact public run receipts and expose the last ten runs with download actions. |
| Privacy of exports | Operator state contains invoice labels, private budgets, reasons and snapshots. | Public run receipt has batch/manifest/decision payload only, plus run provenance. Export tests reject operator fields. Omit full snapshot from routine ledger responses. |
| Failure UI | Network/edge errors were already handled, but reserve semantics were not staged. | Review results explicitly state nothing has been reserved. Reservation and CRE execution remain distinct. |
| Accounting integrity | Invoice identities and PO reservations survive reload; concurrent writes serialized. | Preserve and regression-test existing cross-run and concurrent behavior. |
| CRE meaningfulness | The batch handler evaluates actual authenticated accounting input; a second CLI run consumed the reconciled saved ledger. | Preserve both real simulation and Sepolia evidence. No claim that a browser preview runs CRE. |
| Settlement integrity | Treasury binds ordered requests/amounts/expiry and atomic token transfer; mock adapter pins owner-authorized report. | No untested contract rewrite. Preserve tested replay, rollback and identity checks. |
| Finality | Reconciliation checks canonical receipt block and finalized height from one configured RPC. | Accurate testnet proof; single-RPC trust and test-token scope remain explicit. |
| Practicality | Two fixed synthetic vendor policies, no accounting-provider auth, no arbitrary live treasury integration. | CSV correction and review work; do not represent the demo as deployable business accounting. |
| Originality | Confidential batch policy → exact treasury authorization → reconciled invoice identity is the strongest implementation story. | Focus the demo on that sequence; no unsupported claim that commercial invoice tools lack every feature. |
| Human involvement | Implementation and scripts predominantly generated by Codex. | Customer findings, personal product decisions and narration remain pending; no invented evidence. |
| Live confidentiality | General deployment access requested; separate beta form still needs user details. | Simulation is accepted in the sponsor criteria. Do not claim hardware-protected execution has occurred. |

## Review and reservation semantics

Review does not mutate the ledger. It derives decisions from the current saved state.
The browser submits that revision together with a new request key only when the user
chooses to reserve. Another workspace mutation makes that revision stale. Successful
reservation saves the request's canonical input hash, decisions and public receipt
atomically. Retrying that same request returns the saved result even if the ledger
has since changed. It never creates another reservation. A different draft requires
a new key and a fresh review. Legacy clients without a key retain their existing
server-generated run behavior; concurrency and duplicate protections still apply.

All browser results remain policy previews. Public run receipts are not signed CRE
attestations and do not establish a chain payment. The report's IDs/commitments can
be public while invoice labels and reasons remain operator-only. The actual recorded
CRE and Sepolia artifacts are separate, clearly labeled evidence.

## High-impact work that remains external or out of this prototype's scope

A real treasury-operator interview, human acceptance decisions, human narration,
and final submission are not substituted by automated testing. A live confidential
workflow requires access approval and a real workflow identity/forwarder. Production
accounting requires authenticated organization ownership and safe chain-cancellation
reconciliation. Arbitrary tokens, malicious accounting sources, and private payments
are outside the demonstrated trust model.

## Judging fit

The sponsor lists confidential payment orchestration and accepts a successful CRE
CLI confidential-workflow simulation. The core handler and recorded executions align
with those technical requirements; eligibility and judging remain organizer decisions.

Sources checked September 12:
- https://ethglobal.com/events/ethonline2026/prizes/chainlink
- https://ethglobal.com/events/ethonline2026/info/details

The selected changes address concrete usability, reliability and explanation defects.
They improve the work a judge can inspect; they are not a prize-probability estimate.

## Verified outcome

The first-visit page is now 3,988 px high at 1440×1000 (previously 4,562, about 13%
shorter). Sepolia proof moved from y=2609 to y=688. This measures document layout,
not a performance score or a user-conversion improvement. The working ledger expands
when opened; its horizontal table remains contained at 390 px viewport width.

14 live ledger checks passed, including corrected CSV import, downloadable run
receipts, retry stability, concurrent reservation protection, reload persistence,
and actual finalized Sepolia reconciliation. The existing 18-check live browser
suite passed after opening the newly collapsed technical evidence section.
Targeted unit tests cover read-only review, stale revision rejection, identical-key
retry equivalence, different-input key rejection, and operator-field omission.

## Next iteration: let the reviewer challenge the contract

A separate `/lab.html` now executes nine adversarial and positive-control experiments
against actual compiled treasury bytecode in a browser Web Worker. This addresses a
remaining presentation gap: reviewers previously had to trust recorded contract-test
results or reproduce a CLI environment. The new experience makes exact-payment
binding, complete manifests, sender/workflow checks, replay behavior and transfer
rollback directly inspectable. It does not turn browser execution into CRE execution.
The primary CTA now opens the lab; the persistent accounting desk remains one click
away. The EVM bundle is lazy-loaded and does not affect the main page's initial assets.

## Agent execution pivot — September 12

The opening concept is now a confidential execution gate for AI treasury proposals. A live model receives a synthetic invoice; a separate trusted record controls canonical payment fields and policy. Both clean and malicious invoice responses were captured. The malicious live response selected the attacker wallet despite an untrusted-input system prompt. Three actual CRE CLI simulations and 21 local EVM checks verify a valid transfer and two denied settlement attempts. The homepage and human demo script now lead with that evidence.

The persistent desk moved to `/desk.html` and remains a separate supporting workflow. No claim is made that agent preview calls reserve its budget. Inference envelope handling accepts provider string or structured response while retaining strict proposal validation; malformed output fails closed. Fixed scenarios and globally bounded model calls constrain the public endpoint. Production accounting integration and deployed confidential attestation remain missing.
